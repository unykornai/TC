# OPTKAS Platform Environment Configuration
# Copy to .env and fill in values — NEVER commit .env

# Network (testnet | mainnet)
OPTKAS_NETWORK=testnet

# Config path
OPTKAS_CONFIG=./config/platform-config.yaml

# Dashboard
DASHBOARD_PORT=3000

# XRPL
XRPL_TESTNET_URL=wss://s.altnet.rippletest.net:51233
XRPL_MAINNET_URL=wss://xrplcluster.com

# Stellar
STELLAR_TESTNET_URL=https://horizon-testnet.stellar.org
STELLAR_MAINNET_URL=https://horizon.stellar.org

# Key Management (HSM/KMS references only — NEVER actual keys)
# KMS_PROVIDER=aws-kms | azure-keyvault | hashicorp-vault
# KMS_KEY_ID=<key_id>
# KMS_REGION=<region>

# Compliance (API stubs)
# KYC_PROVIDER_URL=
# AML_PROVIDER_URL=
# SANCTIONS_LIST_URL=

# Logging
LOG_LEVEL=info
LOG_FORMAT=json
